# Core Dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
streamlit==1.28.1
python-dotenv==1.0.0
pydantic>=2.9.0
pydantic-settings
PyPDF2
pdfplumber>=0.11.0

# HTTP & API
httpx>=0.25.0
requests>=2.31.0

# Database
sqlalchemy>=2.0.0
# sqlite3 is built-in to Python, no installation needed

# AI/ML
openai>=1.3.5
# tiktoken is optional - not used in codebase, but OpenAI SDK may use it
# tiktoken>=0.8.0  # Uncomment if needed

# Data Processing
pandas>=2.2.0  # Use newer version with pre-built wheels
numpy>=1.26.0

# Utilities
python-dateutil>=2.8.0
pytz>=2023.3

# Authentication
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.0
streamlit-authenticator>=0.2.0

# Optional: For local LLM support
# ollama==0.1.0  # Uncomment if using local Ollama
